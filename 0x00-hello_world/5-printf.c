#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char mystring[] = "with proper grammar, but the outcome is a piece of art,\n";
	printf("%s\n", mystring);
	return 0;
}
