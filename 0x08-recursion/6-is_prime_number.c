#include "main.h"
/**
 * is_prime_number - function to print prime number
 * @n: an integer n
 *
 * Return: 1 if prime orn 0 if not
 */

int is_prime_number(int n)
{
	if (n <= 1)
		return (0);
	else
		return (1);
}
