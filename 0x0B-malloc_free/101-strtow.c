#include <stdlib.h>
#include "main.h"
/**
 * strtow - that split a string
 * @str: this is address of the string
 *
 * Return: splited world
 */

char **strtow(char *str)
{
	if (*str == '\0')
		return (0);
	return (str);
}
