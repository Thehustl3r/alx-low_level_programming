#include <stdio.h>
/**
 * print_diagsums - print sum of two array dimension
 * @a: an pointer a
 * @size: an integer a
 *
 * Return: Nothing
 */
void print_diagsums(int *a, int size)
{
	int i,j;
	int sum[size];

	i = 0;
	j = 0;
	
	for (; a < size; i++)
	{

	}
	return (sum);
}
